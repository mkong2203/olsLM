% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/olsLM.R
\name{olsLM}
\alias{olsLM}
\alias{olsLM.formula}
\alias{olsLM.default}
\title{Ordinary Least Squares Linear Regression}
\usage{
olsLM(x, ...)

\method{olsLM}{formula}(x, data, ...)

\method{olsLM}{default}(x, y, call = NULL, ...)
}
\arguments{
\item{x}{A model formula (for the formula interface) or a numeric matrix
of predictors (for the matrix interface).}

\item{...}{Additional arguments passed to methods.}

\item{data}{A data frame containing variables used in the formula.}

\item{y}{Response vector (numeric).}

\item{call}{The matched call (for printing).}
}
\value{
An object of class \code{"olsLM"} containing:
\itemize{
\item \code{coefficients} Estimated regression coefficients.
\item \code{fitted} Fitted values.
\item \code{residuals} Ordinary residuals.
\item \code{sigma2} Residual variance estimate.
\item \code{df_residual} Residual degrees of freedom.
\item \code{vcov} Estimated covariance matrix of coefficients.
\item \code{coef_table} Estimate, Std.Error, t-value, p-value.
\item \code{r_squared} R-squared.
\item \code{adjusted_r_squared} Adjusted R-squared.
\item \code{leverage} Hat values.
\item \code{r_standard} Standardized residuals.
\item \code{r_student} Internally studentized residuals.
\item \code{r_student_ext} Externally studentized residuals.
}
}
\description{
\code{olsLM()} fits a linear regression model using ordinary least squares (OLS).
It supports both:
\itemize{
\item \strong{formula interface}: \code{olsLM(y ~ x1 + x2, data)}
\item \strong{matrix interface}:  \code{olsLM(X, y)}
}
}
\details{
The function returns coefficients, fitted values, residuals, leverage,
standardized residuals, internally studentized residuals, and externally
studentized residuals.
}
\examples{
fit1 <- olsLM(mpg ~ wt + hp, data = mtcars)
summary(fit1)

X <- as.matrix(mtcars[, c("wt", "hp")])
y <- mtcars$mpg
fit2 <- olsLM(X, y)

}
